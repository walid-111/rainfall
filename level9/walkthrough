1. Je get le programme avec scp, et le decompile comme d'habitude.
2. Nous pouvons voir que a la fin du programme, la fonction setAnnotation fais un strcpy sans verifier la taille qu'il copie
3. Nous allons donc pouvoir manipuler avec un overflow
4. Pour cela il me faut l'adresse de system et aussi l'adresse de "bin/sh" afin de la bind sur l'adresse de la fonction field_0 retourner par le main un l'adresse de system avec sur l'adresse du premier parametre "bin/sh"
 ```
 gdb ./level9
(gdb) break main
(gdb) run
(gdb) p system
(gdb) info proc mappings -> to get start and end adresse
(gdb) find 0xb7e00000 (start address), 0xb7fc0000( end address), "/bin/sh"
```
-> J'obtiens l'adresse ``0xb7d86060`` pour system et ``0xb7d47000`` -> start_address et  ``0xb7d47000`` -> end_address

Je peux maintenant chercher l'adresse de "bin/sh" dans la libc -> ``find 0xb7d47000, 0xb7eea000, "/bin/sh"`` -> qui me donne -> ``0xb7ea7c58``

5. Je peux maintenant construire l'exploit: 